# AI 能力设计说明 — FreightGo 跨境物流智能询价平台

> 本文档面向 **AI 产品助理面试**，回答一个核心问题：
> **「这个产品里，AI / 大模型到底做了什么？为什么这么设计？」**
>
> 说明：演示版 `index.html` 是纯前端 + 虚拟数据（离线可跑、脱敏），用于证明产品落地；
> 而本文描述的 AI 能力来自**真实生产系统的架构设计**，数据虚拟、架构真实。

---

## 1. 一句话定位

FreightGo 不是「套一个大模型壳」的 Demo，而是用 **「规则引擎兜底 + 大模型补盲」** 的务实思路，
把跨境物流询价里**最贵、最容易出错**的环节交给 AI，把**必须精确、可溯源**的环节留给规则。

**核心原则：宁可不报价，不可报错价。** AI 负责提速，规则负责保命。

---

## 2. AI 能力全景图

| # | 能力 | 用到的模型 / 技术 | 解决什么痛点 | 在演示版中的体现 |
|:--|:--|:--|:--|:--|
| 1 | 截图询价（多模态 OCR） | qwen3.8-max（VLM） | 客户甩来一张截图，人工逐行抄价慢且易错 | 架构已实现，演示版以虚拟数据复现询价链路 |
| 2 | 模型路由 | deepseek-v4-pro / -flash / qwen3.8-max | 大模型贵且慢，小模型快但浅，一刀切浪费算力 | 架构设计章节 |
| 3 | 智能比价与推荐 | 规则 + 排序算法 | 渠道命名五花八门，人工比价难 | ✅ 演示版可见（排名页 🥇🥈🥉） |
| 4 | 计费规则引擎 | 规则库（四维锁定） | 比重/尺寸/报关费/附加费漏算就亏钱 | ✅ 演示版数据含重量段折扣 |
| 5 | 多 Agent 协作 | QwenPaw 多智能体框架 | 查价、报价、监控、展示各司其职 | 架构设计章节 |
| 6 | 价格趋势与异常告警 | SQL 快照对比 | 价表周更，人工盯不住涨跌 | ✅ 演示版可见（趋势页） |
| 7 | 航运周报聚合 | 数据聚合 + 外部指数 | 决策信息散落各处 | ✅ 演示版可见（周报 6 卡片） |

---

## 3. 能力详解

### 3.1 多模态截图询价（OCR + VLM）⭐ 最能体现「AI 落地」的能力

**场景**：商务收到客户一张报价截图（可能是别人家的价表、装箱清单、微信聊天截图），
需要快速识别仓库代码并出我方报价。

**技术链路**：

```
截图 ──▶ qwen3.8-max（VLM 逐字 OCR）──▶ 仓码提取 ──▶ 白名单校验 ──▶ 询价引擎 ──▶ 报价
```

**为什么用 VLM 而不是传统 OCR**：
- 价表是「表格 + 渠道名 + 仓码 + 数字」混排，传统 OCR 只出文字不出结构；
- 多模态模型能同时理解版面语义，对表格、合并单元格、形近字符更鲁棒。

**关键设计：白名单校验兜底（防幻觉）**
- 大模型对形近字符（`O/0`、`I/1`、`M/N`、`N/H`）有**随机识别误差**；
- 识别出的仓码必须过 FBA 白名单校验，白名单外的「疑似仓码」不硬报，而是用**编辑距离**找出相似候选（如 `RMN3↔RNO3`）主动向用户确认。
- 这是「AI 产品」与「玩具」的分水岭：**AI 可以猜，但猜错了必须能被兜住。**

**价值量化**：一次截图询价从「人工抄价 3-5 分钟」降到「秒级」，且错误率由规则兜底。

---

### 3.2 模型路由（大小模型分工）

**问题**：所有请求都丢给最强模型 = 贵 + 慢；所有都丢给小模型 = 复杂任务翻车。

**设计**：按任务复杂度路由到最合适的模型。

| 场景 | 模型 | 理由 |
|:--|:--|:--|
| 普通查价 / 查仓 / 查渠道 | deepseek-v4-flash | 轻量快速，够用 |
| 价表解析 / 数据清洗 / 复杂推理 | deepseek-v4-pro | 深度推理，准确优先 |
| 图片 / 截图识别 | qwen3.8-max | 多模态视觉，pro/flash 无视觉能力 |

**产品视角的思考**：模型路由本质是「**成本与效果的最优分配**」——
这是 AI 产品助理需要具备的**成本意识**，而不只是「会调 API」。

---

### 3.3 智能比价与推荐

**流程**：渠道母集标准化 → 同一渠道归集多供应商 → 底价升序 → 前三名标记 🥇🥈🥉。

**难点不在排序，在于「可比性」**：
- 各供应商渠道名五花八门（如「限时达」「至尊达」「直送专线」），
  必须统一归并到**标准渠道母集**（如「美森CLX正班」「普船卡派」），比价才有意义。
- 原始渠道名与进仓点**保真留存**，报价可溯源到原始价表——这是「不报错价」的根基。

---

### 3.4 计费规则引擎

**为什么不是大模型算钱**：运费涉及比重优惠、尺寸附加费、报关费、按方计费等硬规则，
**一分钱都不能算错**，大模型做算术有概率出错且不可解释。

**设计**：规则库四维锁定（`供应商 + 服务类型 + 渠道组 + 类别`），逐条核：
`单价 ± 比重折扣 ± 尺寸附加费 ± 重量附加费 + 报关费`。

**AI 在这里的角色是「提取」，不是「计算」**：从 Excel 价表里把散落的规则文本
**精确提取**出来结构化入库，计算交给确定性引擎。**AI 做感知，规则做决策。**

---

### 3.5 多 Agent 协作编排

生产系统基于多智能体框架，把职责拆成多个专职 Agent：

| Agent 职责 | 一句话 |
|:--|:--|
| 数据解析入库 | 把 Excel 价表变成结构化数据 |
| 询价查询 | 解析仓库/渠道/规则，喂给报价引擎 |
| 报价计算 | 计费规则 + 报价格式化 |
| 价格监控 | 涨跌检测、异常告警 |
| 数据展示 | 对比表、网页、趋势 |

**产品视角**：Agent 拆分遵循「**单一职责 + 可独立验证**」，
每个 Agent 有明确的输入输出与验收标准，避免「一个大 Prompt 干所有事」的不可维护性。

---

### 3.6 价格趋势与异常告警

- 基于 SQLite 价格快照全量对比，追踪 6 大核心渠道近 4 周走势；
- 数据异常（如「仓库代码数量骤降」）触发最高优先级告警，先查数据再查代码。

---

### 3.7 航运周报聚合

聚合 SCFI / WCI 等运价指数 + 内部价格数据，生成 6 类决策卡片：
运价走势、全球运价指数、本周概览、最优报价、市场预测、重点关注。
数值**动态生成、不硬编码**。

---

## 4. AI 产品设计方法论（本项目的可迁移经验）

面试时可以用这 5 步讲清楚「我是怎么做 AI 产品的」：

1. **场景识别**：先找到「人工最痛、AI 最擅长」的环节（截图询价、比价），而不是为了 AI 而 AI。
2. **能力拆解**：把一个大需求拆成「AI 能做的（感知/提取/生成）」和「规则该做的（计算/校验/兜底）」。
3. **模型选型**：按成本、效果、模态三要素路由，而不是「永远上最强模型」。
4. **防幻觉设计**：任何 AI 输出都过白名单/规则兜底，可溯源、可确认。
5. **验收标准**：每个能力定义可量化的验收（覆盖率 100%、响应 < 1s、差异 ≤ 0.01）。

---

## 5. 关键设计决策与取舍

| 决策 | 选择 | 理由 |
|:--|:--|:--|
| 计算钱用 AI 还是规则？ | 规则 | 钱不能算错，规则可解释、可溯源 |
| OCR 用传统还是 VLM？ | VLM | 表格混排场景需要版面语义理解 |
| 单模型还是多模型路由？ | 多模型路由 | 成本与效果的最优分配 |
| 截图识别错了怎么办？ | 白名单 + 编辑距离兜底 | AI 可以猜，猜错必须被兜住 |
| 单 Agent 还是多 Agent？ | 多 Agent | 单一职责、可独立验证、可维护 |

---

## 6. 局限与下一步（诚实声明）

- **演示版数据为虚拟**：`index.html` 内嵌的是虚拟数据，不调用真实大模型，
  用于证明产品形态与交互；真实 OCR / 模型路由在生产环境跑通。
- **待接入**：加拿大线、截图询价的线上化（GitHub Pages 分片加载已验证可行性）。
- **可深化方向**：自然语言转 SQL 询价（NL2SQL）、语义搜索（中文地名→仓码）、
  基于历史询价日志的智能推荐。

---

## 7. 面试速记（30 秒版）

> 「这个产品的 AI 能力核心是『规则兜底 + 大模型补盲』：截图询价用多模态 VLM 做 OCR、
> 白名单兜底防幻觉；查价/解析/推理按场景路由到 flash / pro / qwen-max 三个模型；
> 而算钱、比价这些必须精确的环节留给确定性规则引擎，保证宁可不报价、不可报错价。」
