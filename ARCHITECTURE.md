# 系统架构设计 — FreightGo 跨境物流智能询价平台

> 本文档展示**技术架构与工程设计的完整链路**，用于面试时体现架构思维与落地能力。
> 说明：演示版 `index.html` 复用本架构的「应用层渲染」与「数据模型」，数据源换为虚拟数据。

---

## 1. 架构总览（分层）

```
┌─────────────────────────────────────────────────────────────┐
│  应用层   询价网页 · 价格对比表 · 趋势图 · 周报              │
├─────────────────────────────────────────────────────────────┤
│  AI 层   截图OCR(多模态) · 模型路由 · 多Agent编排            │
├─────────────────────────────────────────────────────────────┤
│  引擎层   询价引擎 · 报价引擎 · 计费规则引擎                 │
├─────────────────────────────────────────────────────────────┤
│  解析层   统一入库 · 卡派/海派/时效/规则/船期 五模块          │
├─────────────────────────────────────────────────────────────┤
│  数据层   SQLite 三库分离 · 分线路分表 · 配置集中化          │
└─────────────────────────────────────────────────────────────┘
```

**核心思想：数据、规则、AI、展示四层解耦，每一层可独立验证、独立替换。**

---

## 2. 数据流

```
供应商价表(Excel)
      │
      ▼
解析层（统一入库入口）
      │  卡派 / 海派 / 时效 / 规则 / 船期 五大模块提取
      │  提取器输出 vs Excel 源文件 全量校验（覆盖率 100%，差异 ≤ 0.01）
      ▼
数据层（SQLite）
      │  生产库(只读报价) / 测试库(技能产出) / 档案库(趋势历史)
      │  价格分线路分表：美线 / 欧线 / 加拿大线
      ▼
引擎层（询价引擎 → 报价引擎）
      │  渠道母集标准化 + 进仓点别名展开 + 计费规则逐条核
      ▼
应用层（渲染 → 询价网页）
      │  询价 / 比价 / 排名 / 趋势 / 周报
      ▼
商务用户
```

---

## 3. 分层详解

### 3.1 数据层：三库分离 + 分线路分表

| 库 | 职责 | 铁律 |
|:--|:--|:--|
| 生产库 | 报价/查询/对比表只读 | 仅人工批准后写入 |
| 测试库 | 技能产出、新价表、新网页默认落库 | 实验室 |
| 档案库 | 趋势图历史 | 旧数据封存 |

**分线路分表**：美线 / 欧线 / 加拿大线物理隔离，**禁止跨线路查询**，从根上避免脏数据。

**配置集中化**：所有跨模块配置（供应商进仓点、冻结名单、沿用名单、别名映射）
统一收敛到配置中心单例源头，一处修改全局生效。

### 3.2 解析层：统一入库 + 模块化提取

- 唯一入库入口，五大模块各司其职（卡派 / 海派 / 时效 / 规则 / 船期）；
- 每个提取器返回带行列定位的数据，支持**全量验证**（100% 覆盖率，非抽样）；
- 仓码用**白名单 + 黑名单**校验（禁用正则猜），渠道用**母集对照表**严格归类；
- 数据入库后自动触发**价格监控**（涨跌对比）。

### 3.3 引擎层：询价 + 报价

- 询价引擎：仓库/渠道解析 + 进仓点别名反向展开 + 重量档位匹配；
- 报价引擎：计费规则逐条核（比重折扣 / 尺寸附加 / 报关费 / 按方计费）；
- **底价原则**：报价只读数据库原始底价，禁止任何加价。

### 3.4 AI 层：规则兜底 + 大模型补盲

详见 [`AI_FEATURES.md`](./AI_FEATURES.md)。一句话：
**AI 负责感知（OCR / 提取 / 推荐），规则负责决策（算钱 / 校验 / 溯源）。**

### 3.5 应用层：单文件离线 + 线上分片

- 演示版：构建脚本注入虚拟数据 → 单文件 `index.html`，双击即用，零部署成本；
- 线上版：GitHub Pages 分片加载，数据更新无需重新下载（已验证可行性）。

---

## 4. 关键技术选型

| 层 | 选型 | 理由 |
|:--|:--|:--|
| 前端 | 原生 HTML/CSS/JS，Chart.js 内嵌 | 单文件、无 CDN 依赖、离线可用 |
| 数据 | SQLite | 零运维、可移植、适合商务单机场景 |
| 入库 | Python 提取器 + 冲突更新 | 可验证、可回滚、防重复 |
| AI | 多模型路由（大小模型 + 多模态） | 按场景路由，成本与效果最优 |
| 编排 | 多智能体框架 | 单一职责、可独立验证 |

---

## 5. 关键工程决策

| 决策 | 方案 | 价值 |
|:--|:--|:--|
| 三库分离 | 生产/测试/档案物理隔离 | 测试改动不污染生产，历史可追溯 |
| 分线路分表 | 美线/欧线/加线分表 | 跨线路脏数据从根上杜绝 |
| 原始名保真 | 渠道原始名与进仓点保留原文 | 报价可溯源，不报错价 |
| 底价原则 | 引擎只读底价，禁止加价 | 宁可不报价，不可报错价 |
| 配置集中化 | 配置中心单例 | 改一处全局生效，杜绝副本漂移 |
| 全量验证 | 提取器输出 vs Excel 源文件逐格比对 | 数据可信，AI 才敢用 |

---

## 6. 工程质量保障体系（三层防线 + 工程骨架）

> 系统不只是「能跑」，而是把「不出错」做成了工程机制。这是本项目的核心亮点之一。

### 6.1 三层防线：源头 → 过程 → 兜底

```
① 源头约束        ② 运行中校验              ③ 风险管控
（入库前结构校验）  （动态基线 + 有限重试）   （预拆解 + 确认 + 熔断）
```

**① 源头约束 —— 非法数据进不来**
- 入库命令参数 + 入库记录结构双重校验（必填字段 / 类型 / 合法取值枚举）；
- 非法参数当场拒绝，不进入数据库。

**② 运行中校验 + 有限重试 + 循环检测**
- **动态基线校验**：以历史数据建立基线（滑动窗口），本次数量骤降 50% 即告警——不再依赖写死的阈值；
- **有限重试**：连续 N 次异常才确认失败，中间恢复则判定为抖动（重试的是「校验/重查确认」，绝不是重做入库，避免重复插入）；
- **循环检测**：记录调用指纹，相同命令连续失败即判无限循环、终止。

**③ 风险管控：预拆解 + 高风险确认 + 超时熔断**
- **任务预拆解**：复杂任务动手前先列「需调用哪些工具 + 读哪些文档」；
- **高风险二次确认**：改/删生产数据、回滚、迁移归档，必须先人工确认；
- **超时熔断 + 降级**：持续报错或超时卡死时熔断终止，防止错误扩散。

### 6.2 工程骨架四环节

| 环节 | 职责 | 落地形式 |
|:--|:--|:--|
| 测试门禁 | 改动必须过自动化断言 | 38 项断言，覆盖入库/查询/船期/规则/监控五链路 |
| 数据哨兵 | 数据异常主动告警 | 基线骤降检测 + 故障注入验证 |
| 流水线编排 | 任务受控执行 | 守卫包装 + 循环/熔断检测 |
| 迁移 + 回滚 | 数据迁移可回退 | 生产数据迁移前归档，异常可回滚 |

### 6.3 代码版本管理

- **多仓库隔离**：工程骨架 / 核心业务 / 询价网页 / 演示版 分仓库独立提交与回滚；
- **最小权限凭证**：细粒度凭证仅授权指定仓库的内容读写，仓库管理等高权限操作由人工在网页完成；
- **双分支对齐**：统一默认分支命名，消除分叉。

---

## 7. 可扩展性设计

- **新增供应商**：写一个提取器 + 登记进供应商配置，前端自动适配（不硬编码供应商名）；
- **新增线路**：加一张分表 + 路由映射，不影响既有线路；
- **新增 AI 能力**：在 AI 层扩展（如 NL2SQL、语义搜索），不动数据层与引擎层。

---

## 8. 与演示版的关系（诚实说明）

| 组件 | 真实系统 | 演示版 |
|:--|:--|:--|
| 数据 | 供应商 Excel 价表（周更） | 虚拟数据（固定种子可复现） |
| 解析/入库 | 统一入库全链路 | 不在 demo 中运行 |
| 询价/比价/趋势/周报 | 同一套渲染逻辑 | ✅ 复用，数据虚拟 |
| AI（OCR/路由/Agent） | 生产环境跑通 | 架构文档说明，demo 不调用 |

> 演示版的价值在于**证明「产品形态 + 交互 + 数据模型 + 工程质量」可落地**；
> 真实的解析入库管道与 AI 能力在生产系统运行，面试时可据本文档展开讲解。
