# 项目作品集 — FreightGo 跨境物流智能询价平台

> 本文档是 [FreightGo_项目作品集.pdf](./FreightGo_项目作品集.pdf) 的 Markdown 配套版，
> 用于面试时对照讲解。项目名与数据均为虚拟编撰，不包含任何真实公司信息。

---

## 1. 项目概述

**一句话价值**：把跨境物流「人工翻表查价」升级为「秒级询价 + 智能比价 + 趋势洞察」的垂直 AI 询报价系统。

**核心价值**：提效（数分钟 → 秒级）、降错（报价可溯源）、赋能决策（比价/趋势/周报）。

**关键指标**：6 万+ 活跃报价 · 15 家供应商 · 380+ 去重仓库 · 100% 价表验证覆盖率 · <1s 查询响应。

---

## 2. 我在项目中的「产品」工作

### 2.1 需求分析：把模糊的「查价太慢」拆成可落地的功能

| 原始诉求 | 拆解出的真问题 | 落地功能 |
|:--|:--|:--|
| 查价太慢 | 价格散落数十张周更 Excel | 仓库代码一键询价，秒级返回 |
| 比价难 | 渠道命名、计费规则不统一 | 渠道母集标准化 + 最低价归集 |
| 报价乱 | 起运地/时效/附加费口径不一 | 报价格式统一 + 附加费逐条列 |

### 2.2 功能设计：定义「输入 → 输出 → 规则 → 验收标准」

每个功能都定义了**可量化的验收标准**（搜索响应 < 1 秒、比价与数据库底价一致、
提取覆盖率 100%、差异 ≤ 0.01），体现「不满足于能跑，而是可验证、可量化」。

### 2.3 数据设计：为 AI 落地打地基

- 设计「供应商 × 渠道 × 仓库 × 重量段」四维报价数据模型；
- 三库分离（生产/测试/档案）+ 分线路分表（美线/欧线/加拿大线）隔离；
- 原始渠道名与进仓点保真留存，保证报价可溯源——这是 AI 能放心用的前提。

### 2.4 AI 能力规划：知道「AI 该做什么、不该做什么」

- 判断哪些环节适合 AI（截图 OCR、比价推荐、趋势洞察）；
- 判断哪些环节必须用规则兜底（算钱、校验、报价溯源）；
- 设计防幻觉机制（白名单校验 + 编辑距离兜底）、模型路由（成本与效果最优分配）。

详见 [AI_FEATURES.md](./AI_FEATURES.md)。

---

## 3. 架构设计的考虑与探索点（核心亮点）

> 这是本项目最有说服力的部分：**架构的每一处设计都能讲清「为什么」以及「是哪个坑逼出来的」。**

### 3.1 演进主线：从「脚本跑通」到「工程系统」

| 阶段 | 状态 | 暴露的问题 | 催生的机制 |
|:--|:--|:--|:--|
| 脚本集合 | 核心链路能跑通 | 「能跑」不等于「不出错」 | —— |
| 抽象铁律 | 把踩过的坑沉淀成规则 | 靠人肉记忆，换人就丢 | 规则写进文档，成为唯一权威 |
| 工程化 | 测试门禁 + 哨兵 + 编排 + 回滚 | 人工巡检有盲区 | 把铁律变成自动断言与告警 |

### 3.2 架构设计的「考虑」——每个决策背后的为什么

| 设计决策 | 背后的考虑 |
|:--|:--|
| 三库分离（生产/测试/档案） | 测试改动不污染生产；报价只读；历史可追溯 |
| 分线路分表（美/欧/加） | 跨线路脏数据从根上杜绝 |
| 配置集中化 + 决策日志 | 跨模块配置漂移是隐性 bug 之源 |
| 原始名保真（渠道名 + 进仓点） | 报价可溯源，是「不可报错价」的根基 |
| 底价原则 | 报价引擎只读数据库原始底价，禁止加价 |
| 唯一入口（格式化 / 查询） | 防手写漂移，格式与字段永远一致 |
| 白名单 + 黑名单 + 编辑距离 | AI 幻觉兜底 |

### 3.3 探索点：从踩坑到机制（真实演进历程）

| 踩过的坑 | 根因 | 沉淀的机制 |
|:--|:--|:--|
| 复合进仓点被静默截断 | 模糊匹配函数缺「含 / 直接返回」保护，复合值被截成单体 | 入库后三重核查 + 原文保真铁律 |
| 双写入函数互删数据 | 两个入库函数各自 DELETE，后者覆盖前者 | DELETE 只执行一次、放最前、可审计 |
| 报价格式三次复发 | 凭手感手写，没走唯一格式化入口 | 「唯一入口」模式：报价一律走 format_quote |
| 数据量骤降未被发现 | 靠人眼撞见异常，发现时已污染 | 数据哨兵：动态基线 + 骤降告警 |
| 归档数据重复膨胀 | 反复入库导致重复归档 | 归档治理 + 数据库瘦身 |

**共性规律**：这些坑都不是「能力问题」，而是「边界条件没兜住」。所以架构核心思路不是
「把代码写对」，而是「**让错误在变成事故前被自动拦截**」。

---

## 4. 工程质量保障：三层防线

让错误在变成事故前被自动拦截，而非依赖人肉巡检。

- **① 源头约束**：入库前 JSON Schema 校验（必填/类型/合法取值枚举），非法数据当场拒绝。
- **② 运行中校验**：动态基线（数量骤降告警）+ 有限重试（重查确认，不重做入库）+ 循环检测。
- **③ 风险管控**：任务预拆解 + 高风险二次确认 + 超时熔断降级。

工程骨架四环节（最小可运行，已验证可行性）：**测试门禁（38 项断言）· 数据哨兵 · 流水线编排 · 迁移回滚**。

---

## 5. 成功经验与复刻优势

### 5.1 成功经验：验证了什么跑通

- **可行性闭环**：用「最小可运行四环节」验证「自动化兜底」走得通（测试门禁 13 项断言、哨兵 8 项检查、流水线三步全绿）。
- **数据可信**：提取器输出与 Excel 逐格比对，100% 覆盖、差异 ≤ 0.01，让 AI 敢放心用。
- **演进路径可复制**：脚本跑通 → 抽象铁律 → 工程固化，每一步可验证、可回退。

### 5.2 复刻优势：方法论能迁移到哪

| 可复刻的方法论 | 适用场景 | 为什么能迁移 |
|:--|:--|:--|
| 规则兜底 + AI 补盲 | 任何「AI + 确定性业务」（对账/审查/单据） | 算钱算账给规则，感知提取给 AI |
| 白名单 + 编辑距离防幻觉 | 任何 AI 输出需校验的场景 | AI 可以猜，猜错必须被兜住 |
| 三库分离 + 配置集中化 | 任何数据密集项目 | 生产/测试隔离 + 单一配置源 |
| 三层防线质量体系 | 任何「不出错」是硬要求的系统 | 源头校验 → 过程校验 → 风险管控 |
| 最小可运行骨架验证可行性 | 任何从 0 到 1 的工程落地 | 先跑通核心再工程化 |
| 从踩坑到铁律的沉淀 | 任何团队经验沉淀 | 每个 bug 抽象成规则，固化进机制 |

**一句话**：这套方法论解决的不是「跨境物流询价」一个问题，而是「**如何在 AI 时代做一个
不出错、可溯源、能落地到生产的垂直工具**」这一类问题。

---

## 6. 未来发展路线

### 6.1 项目局限（诚实声明）

- 数据获取有门槛（供应商价表是商业敏感信息，靠商务关系维护接入）；
- AI 只做感知不做决策（报价零容错，规则靠人工沉淀）；
- 单机架构（SQLite + 单文件 HTML，多用户并发/权限未覆盖）；
- 边界规则待补齐（加拿大线待接入、海派 CBM 计费边界规则在完善）。

### 6.2 未来演进方向

| 方向 | 演进内容 | 价值 |
|:--|:--|:--|
| 单机 → 服务化 | API 化 + 多租户 + 权限管理 | 支撑团队协作与远程访问 |
| 美线 → 全线路 | 欧线 / 加拿大线全量接入，统一计费模型 | 一个系统通吃多线路 |
| 感知 → 决策 | 规则 + 模型共学，NL2SQL 自然语言询价 | 降低使用门槛，AI 学会新渠道归类 |
| 工具 → 平台 | 从询价扩展到订舱 / 跟踪 / 结算全流程 | 从单点提效到流程闭环 |
| 可观测性 | 查询日志、模型路由耗时、识别准确率监控 | 让 AI 效果可度量、可优化 |

---

## 7. 项目 → 岗位能力映射

| AI 产品助理能力要求 | 本项目对应证据 |
|:--|:--|
| 需求分析 | 三大痛点拆解为 5 大功能、7 项 AI 能力 |
| 功能设计 | 每项功能定义输入/输出/规则/验收标准 |
| 数据敏感度 | 四维报价模型、三库分离、分线路分表 |
| AI 技术理解 | 模型路由、VLM 截图 OCR、防幻觉、多 Agent |
| 成本与效果权衡 | 按场景路由大小模型，不浪费算力 |
| 工程思维 | 三层防线、最小可运行骨架、从踩坑到铁律 |
| 可量化思维 | 覆盖率 100%、响应 <1s、差异 ≤0.01 |
| 商业思维 | 核心价值「提效、降错、赋能决策」+ 未来演进路线 |

---

## 8. 面试思考素材（被追问时的「我的思考」）

1. **「为什么不用大模型算钱」** —— 钱一分不能错，大模型算术有概率出错且不可解释，
   所以坚持「规则引擎算钱、AI 做感知提取」的分工，体现对 AI 能力边界的清醒认知。
2. **「怎么防 AI 截图识别出错」** —— 大模型对形近字符（O/0、I/1、M/N）有随机误差，
   用白名单校验 + 编辑距离兜底，不确定时主动向用户确认，而非硬报。
3. **「怎么让 8 家供应商报价可比」** —— 渠道命名五花八门，做渠道母集标准化，
   同时保留原始名，做到既可比、又可溯源。
4. **「架构为什么这么设计」** —— 每一处设计背后都有一个真实踩过的坑，不是凭空画图，
   而是「错误逼出来的机制」——这比「我设计得很完美」更有说服力。
