# 产品需求文档（PRD）— FreightGo 跨境物流智能询价平台

| 项目 | 内容 |
|:---|:---|
| 文档版本 | v1.1 |
| 编写日期 | 2026-08-15 |
| 产品名称 | FreightGo 跨境物流智能询价平台 |
| 文档状态 | 演示版（应聘配套） |

---

## 1. 背景与问题

跨境物流商务人员在日常报价中面临三大痛点：

1. **查价慢**：供应商价格分散在数十张 Excel 价表中，人工翻表核对耗时长，且价表按周更新、版本繁多。
2. **比价难**：各供应商渠道命名、计费规则（重量段 / 比重 / 附加费 / 报关费）不统一，难以横向比较真实底价。
3. **报价乱**：报价口径（起运地、时效、附加费）不一致，容易报错价、漏要素，影响成交与口碑。

**核心判断**：这三个痛点不是「缺少工具」，而是「数据非结构化 + 口径不统一 + 计算不精确」。因此产品不做一个「大模型套壳」，而是先解决「数据可结构化、可比、可溯源」，再把 AI 用在真正提速的环节。

---

## 2. 产品目标

- **提效**：将单次询价从「数分钟翻表」压缩到「秒级查询」。
- **降错**：统一渠道归类与计费规则，确保报价可溯源、不报错价。
- **赋能决策**：提供比价、趋势、周报等数据支撑，辅助商务选品与定价。

---

## 3. 成功指标（怎么衡量「做成了」）

> 产品目标必须可量化，否则「提效、降错」只是口号。

### 3.1 北极星指标

**日均有效询价次数**——衡量商务是否真的把系统用进了日常工作（用了才证明解决了痛点）。

### 3.2 效率指标

| 指标 | 目标值 |
|:--|:--|
| 单次询价耗时 | 从「数分钟翻表」降到 < 1 秒 |
| 查询响应时间 | < 1 秒 |
| 比价出结果 | 秒级，无需二次整理 |

### 3.3 质量指标（降错）

| 指标 | 目标值 |
|:--|:--|
| 价表提取覆盖率 | 100%（全量校验，非抽样） |
| 提取 vs 源文件差异 | ≤ 0.01（视为一致） |
| 报价可溯源率 | 100%（每条报价可回溯到原始价表） |
| 报错价次数 | 0（宁可不报价，不可报错价） |

### 3.4 规模指标（对外模糊口径）

活跃报价 6 万+ · 供应商 15 家（美线）· 去重仓库 380+（精确值见内部材料）。

---

## 4. 目标用户与场景

| 用户 | 场景 | 核心诉求 |
|:---|:---|:---|
| 商务/销售 | 客户询价 | 快速查底价、比价、出推荐方案 |
| 管理层 | 经营决策 | 查看价格趋势、供应商排名、市场周报 |
| 数据/运营 | 价表维护 | 入库新价表、校验数据质量、监控价格变动 |

---

## 5. 用户故事（核心场景）

1. **作为商务**，我想输入一个 FBA 仓库代码（如 `ONT8`）就拿到多家供应商报价，以便秒级响应客户询价。
2. **作为商务**，我想看到同一渠道下多家供应商的底价对比，以便一眼找到最便宜、时效最快的方案。
3. **作为商务**，我想在客户只给仓库、没给重量/体积时也能先出一个参考价，以便不错过询价窗口。
4. **作为主管**，我想看到 6 大核心渠道近 4 周价格走势，以便判断运价是涨是跌、何时调渠道。
5. **作为数据运营**，我想把供应商新价表入库后自动校验质量、自动对比涨跌，以便及时发现异常价格。

---

## 6. 功能需求

### 6.1 询价查询（核心）

**描述**：输入 FBA 仓库代码（或邮编/国家），返回多供应商、多渠道报价。

- **输入**：仓库代码（如 `ONT8`）、线路（美线卡派/海派/欧线）、重量档位、起运地、目的港。
- **输出**：每条报价含 `供应商 | 渠道 | 重量档位 | 单价 | 单位 | 税费类型 | 时效 | 进仓地`。
- **规则**：
  - 同一渠道只展示最低价档位，避免无效数据堆砌。
  - 未指定重量时默认取最低入门档位。
  - 未指定进仓地时同时展示华东 / 华南双仓对比。
  - 信息不全时输出免责话术（「该价格只是供应商价表 FBA 仓价格，实际价格请给更多有效信息」）。
- **验收标准**：搜索响应 < 1 秒，结果可点击展开比价详情。

### 6.2 智能比价

**描述**：对同一渠道归集多家供应商报价，自动取最低价并给出前三名。

- **规则**：按渠道分组 → 每渠道供应商按底价升序 → 前三名标记 🥇🥈🥉。
- **输出**：渠道排名卡片 + 最低价、时效、供应商对比。
- **验收标准**：比价结果与数据库底价一致，前三名排序正确。

### 6.3 价格趋势

**描述**：追踪核心渠道近 4 周价格走势，辅助判断涨跌。

- **数据**：6 大核心渠道 × 多家供应商 × 4 周价格序列。
- **交互**：缩略图 + 点击放大 + 图例切换。
- **验收标准**：趋势图数据可溯源到历史价表，折线无断裂。

### 6.4 航运周报

**描述**：聚合运价指数与市场情报，形成 6 类决策卡片。

- **6 卡片**：运价走势（SCFI）、WCI 全球运价、本周概览、最优报价、市场预测、重点关注。
- **验收标准**：6 卡片齐全，数值动态生成、不硬编码。

### 6.5 数据管理（入库）

**描述**：供应商 Excel 价表解析入库，含卡派、海派、船期、规则四大模块。

- **流程**：解析 → 验证（100% 覆盖率）→ 入库 → 同步供应商表 → 价格监控。
- **验收标准**：提取器输出与 Excel 源文件单元格值差异 ≤ 0.01 视为一致，覆盖率 100%。

### 6.6 截图询价（AI 能力，规划中）

**描述**：客户甩来一张报价截图，系统识别仓库代码并出我方报价。

- **技术链路**：截图 → 多模态 VLM 逐字 OCR → 仓码提取 → 白名单校验 → 询价引擎 → 报价。
- **防幻觉**：识别出的仓码必须过 FBA 白名单，白名单外「疑似仓码」用编辑距离找相似候选，主动向用户确认。
- **验收标准**：价格识别准确，仓码疑似时主动确认而非硬报。

---

## 7. 需求优先级（P0 → P4）

| 优先级 | 需求 | 价值 | 状态 |
|:--|:--|:--|:--|
| P0 | 美线卡派询价 + 比价 | 核心闭环，验证「秒级询价」可行 | ✅ 已落地 |
| P1 | 美线海派 + 欧线接入 | 覆盖更多线路，扩大适用面 | ✅ 已落地 |
| P2 | 价格趋势 + 航运周报 | 从「查价」升级到「决策」 | ✅ 已落地 |
| P3 | 加拿大线 + 截图询价 | 补全线路 + 降门槛（截图即询价） | ⏳ 规划中 |
| P4 | 线上自动更新（GitHub Pages 分片加载） | 数据更新零下载 | ⏳ 规划中 |

**排序逻辑**：先打透「询价 + 比价」这个最高频刚需（P0/P1），再叠加「决策类」能力（P2），
最后做「增量场景」（P3/P4）。不为炫技而加功能，每一步都能量化价值。

---

## 8. 非功能需求

| 维度 | 要求 |
|:---|:---|
| 性能 | 查询响应 < 1 秒；支持 10 万+ 条价格数据 |
| 可用性 | 单文件离线可用；移动端响应式适配 |
| 安全 | 数据三库隔离（生产/测试/档案）；密钥不入库、不硬编码 |
| 可维护 | 配置集中化管理；新增供应商/渠道自动适配前端 |
| 合规 | 报价可溯源到原始价表；旧数据归档保留 4 周 |

---

## 9. 数据需求

- **数据来源**：供应商周更 Excel 价表（卡派 Sheet / 海派 Sheet / 独立船期表）。
- **数据范围**：美线（卡派 + 海派）、欧线、加拿大线（待接入）。
- **核心字段**：仓库代码、供应商、渠道、重量档位、单价、单位、税费类型、进仓点、时效、船期。

---

## 10. 里程碑规划

| 阶段 | 内容 | 状态 |
|:---|:---|:---|
| P0 | 美线卡派询价 + 比价 | ✅ 已落地 |
| P1 | 美线海派 + 欧线接入 | ✅ 已落地 |
| P2 | 价格趋势 + 航运周报 | ✅ 已落地 |
| P3 | 加拿大线 + 截图询价 | ⏳ 规划中 |
| P4 | 线上自动更新（GitHub Pages 分片加载） | ⏳ 规划中 |

---

## 11. 风险与边界（诚实声明）

| 风险 / 边界 | 说明 | 应对 |
|:--|:--|:--|
| 数据获取门槛 | 供应商价表是商业敏感信息 | 靠商务关系维护接入，先内部用 |
| 价表时效性 | 价表周更，过期价表易报错价 | 价表有效期铁律 + 新文件阻断旧数据 |
| AI 可靠性 | 大模型识别/生成有概率出错 | 白名单 + 编辑距离兜底，算钱走规则 |
| 单机架构 | SQLite + 单文件，未覆盖并发/权限 | 服务化演进路线，按需迁移 |
| 边界规则待补 | 加拿大线、海派 CBM 计费边界 | 已列入 P3/P4 规划 |

**一句话**：产品的边界意识是「不报错价」的延伸——宁可明确说「还没接入」，
也不在边界外硬给一个不可信的结果。
