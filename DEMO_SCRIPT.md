# 面试演示讲解脚本（10 分钟）

> 演示对象：`index.html`（双击打开）
> 讲解节奏：背景 → 演示 → 技术 → 总结。全程可照着念，括号内为操作提示。

---

## 开场（30 秒）

"大家好，我演示的是一套**跨境物流智能询价平台**，解决的是货代商务日常报价的三个痛点：

1. **查价慢** —— 供应商价格散落在几十张 Excel 价表里，人工翻表一次要几分钟；
2. **比价难** —— 各家渠道命名、计费规则不统一，很难横向比较真实底价；
3. **报价乱** —— 起运地、时效、附加费口径不一，容易报错价、漏要素。

这套系统把「翻表查价」变成了「秒级询价 + 智能比价」。下面我直接演示。"

---

## 功能演示（约 6 分钟）

### 1. 首页定位（30 秒）

（打开 index.html，指向首页 Hero 区）

"这是平台首页，核心是一个搜索框。下方动态展示了当前的数据规模：8 家供应商、30 个 FBA 仓库、4000+ 条有效报价。这些数字不是写死的，都是从数据库实时算出来的。"

### 2. 询价 + 比价（2 分钟）

（在搜索框输入 `ONT8`，回车）

"我输入一个 FBA 仓库代码，比如美西的 `ONT8`。系统瞬间返回多家供应商的报价。

每一条报价都包含五个核心要素：**渠道、重量档位、单价、开船后时效、进仓地**。

（点击某条报价，展开比价）

点开任意一条，可以看到**同一渠道下多家供应商的底价对比**，系统自动取了最低价。这样商务一眼就能看到谁最便宜、谁时效最快。"

### 3. 线路切换（1 分钟）

（点击顶部「欧洲」Tab，输入 `DTM2`）

"系统不只有美线。切换到欧洲线路，输入德国仓 `DTM2`，可以看到欧线卡航、海运、铁路、空运的报价，税费类型区分了 DDP 和 DDU。

海派线路同样支持按邮编、按国家的查询。"

### 4. 排名页（1 分钟）

（点击导航「排名」）

"这是渠道排名页，每个渠道自动按底价排出前三名，🥇🥈🥉 标记。商务做推荐方案时，直接看这个页就够了。"

### 5. 趋势 + 周报（1.5 分钟）

（点击「趋势」，再点「周报」）

"趋势页追踪 6 大核心渠道近 4 周的价格走势，点击缩略图可以放大，图例可以切换供应商，用来判断运价是涨是跌。

周报页聚合了 6 类决策信息：运价走势、全球运价指数、本周概览、最优报价、市场预测和重点关注。这些都是动态生成的，不是写死的静态页。"

---

## 技术亮点（约 2 分钟）

（回到桌面，切到代码/文档）

"再说几个技术上的设计：

1. **单文件离线可用** —— 整个网页是一个 HTML 文件，数据内嵌，双击就能打开，没有服务器依赖，部署成本为零。

2. **三库分离 + 分线路分表** —— 生产库、测试库、档案库物理隔离；美线、欧线、加拿大线价格分表存储，从根上避免跨线路脏数据。

3. **渠道母集标准化** —— 各供应商五花八门的渠道名，统一归并到标准渠道母集，比价才有可比性。原始名和进仓点都做了保真，报价可溯源。

4. **底价原则** —— 报价引擎只读数据库原始底价，禁止任何加价，确保不报错价。

5. **Chart.js 本地内嵌** —— 图表库直接打进网页，不依赖 CDN，断网也能正常显示趋势图。"

---

## 总结（30 秒）

"总结一下：这套系统把跨境物流询价从『人工翻表』升级为『秒级询价 + 智能比价 + 趋势洞察』，核心价值是**提效、降错、赋能决策**。

目前美线、欧线已经跑通，加拿大线和截图询价在规划中，线上自动更新方案也已经验证过可行性。

这个演示版的数据是我虚拟编撰的，用于展示系统的可行性和落地性；真实的系统用的是一套完整的数据解析入库管道，供应商的 Excel 价表每周自动解析、校验、入库。谢谢！"

---

## 可能的追问与应对

| 追问 | 应对要点 |
|:---|:---|
| 数据从哪来？ | 供应商每周发的 Excel 价表，通过解析管道自动入库，100% 覆盖率校验 |
| 怎么保证不报错价？ | 底价原则 + 渠道母集标准化 + 报价可溯源到原始价表 |
| 时效怎么来的？ | 时效来自价表内时效列，三级优先级：价表时效 > 船期表 > 默认时效 |
| 数据量多大？ | 真实系统活跃 6 万+ 条报价，15 家供应商，380+ 个去重仓库 |
| 怎么部署？ | 单文件 HTML 直接分发；线上版用 GitHub Pages 分片加载，实现自动更新 |
| 计费规则怎么处理？ | billing_rules 表四维锁定（供应商+服务类型+渠道组+类别），比重/尺寸/报关费/附加费逐条核 |
